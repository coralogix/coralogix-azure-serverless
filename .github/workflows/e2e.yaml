name: E2E Tests

on:
  # Called by "Build, Test & Release" once it knows which packages it built.
  # That workflow already computes the package list, so e2e is told what to test
  # rather than inferring it -- an earlier version keyed off the name of the
  # upstream workflow, which silently stopped covering three packages the moment
  # the two build workflows were merged into one.
  workflow_call:
    inputs:
      packages:
        description: "JSON array of package names to test"
        required: true
        type: string
      ref:
        description: "Commit to test, and the ref the ARM templates are fetched from"
        required: true
        type: string
  workflow_dispatch:
    inputs:
      package:
        description: "Package to test (all = every package)"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - BlobViaEventGrid
          - BlobToOtel
          - DiagnosticData
          - EventHub
          - StorageQueue

# No workflow-level concurrency group. GitHub keeps only one pending run per
# group even with cancel-in-progress: false, so a shared group would drop
# queued e2e runs. Resource groups are unique per GITHUB_RUN_ID, so overlapping
# runs do not collide.

permissions:
  contents: read
  id-token: write   # required for Azure OIDC / federated login

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.set-packages.outputs.packages }}
      has-changes: ${{ steps.set-packages.outputs.has-changes }}
    steps:
      - name: Set packages matrix
        id: set-packages
        env:
          EVENT: ${{ github.event_name }}
          CALLER_PACKAGES: ${{ inputs.packages }}
          INPUT_PACKAGE: ${{ github.event.inputs.package }}
        run: |
          set -euo pipefail

          if [[ "$EVENT" == "workflow_dispatch" ]]; then
            # Manual runs test one package, or all of them.
            if [[ -n "${INPUT_PACKAGE:-}" && "$INPUT_PACKAGE" != "all" ]]; then
              packages="[\"$INPUT_PACKAGE\"]"
            else
              packages='["EventHub","BlobToOtel","BlobViaEventGrid","DiagnosticData","StorageQueue"]'
            fi
          else
            # Called: test exactly what the caller built.
            packages="${CALLER_PACKAGES:-[]}"
          fi

          echo "packages=$packages" >> "$GITHUB_OUTPUT"
          if [[ "$packages" == "[]" ]]; then
            echo "has-changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has-changes=true" >> "$GITHUB_OUTPUT"
          fi
          echo "Testing: $packages"

  e2e:
    name: e2e (${{ matrix.package }})
    needs: detect-changes
    if: needs.detect-changes.outputs.has-changes == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        package: ${{ fromJson(needs.detect-changes.outputs.packages) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          # Test the commit the caller built, not whatever master is now.
          ref: ${{ inputs.ref || github.sha }}

      - name: Resolve template ref
        id: ref
        env:
          REF: ${{ inputs.ref || github.sha }}
        run: echo "sha=$REF" >> "$GITHUB_OUTPUT"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.4"

      - name: Setup Python (EventHub e2e)
        if: matrix.package == 'EventHub'
        run: pip install azure-eventhub

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Run E2E test
        working-directory: ./${{ matrix.package }}/tests
        env:
          OTEL_ENDPOINT: ${{ secrets.OTEL_ENDPOINT }}
          CORALOGIX_API_KEY: ${{ secrets.CORALOGIX_API_KEY }}
          CORALOGIX_QUERY_API_KEY: ${{ secrets.CORALOGIX_QUERY_API_KEY }}
          CORALOGIX_DIRECT_MODE: "true"
          # Deploy the ARM template from the commit under test, not master.
          ARM_TEMPLATE_REF: ${{ steps.ref.outputs.sha }}
        run: ./e2e.sh

      - name: Notify Slack
        if: always() && !cancelled()
        continue-on-error: true
        uses: slackapi/slack-github-action@v4.0.0
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            text: "${{ job.status == 'success' && format('*{0}* - Azure e2e passed', matrix.package) || format('*{0}* - Azure e2e failed', matrix.package) }}"
            attachments:
              - color: "${{ job.status == 'success' && 'good' || 'danger' }}"
                blocks:
                  - type: section
                    text:
                      type: mrkdwn
                      text: "${{ job.status == 'success' && ':white_check_mark:' || ':x:' }} <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View GitHub workflow run>"
